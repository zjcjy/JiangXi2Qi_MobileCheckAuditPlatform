var User, Util, crypto, settings, trade, validateInput;

module.exports = function(app) {
    app.get('/', function(req, res) {
        return res.redirect('login.html');
    });

    app.get('/main', function(req, res) {
        /*var _ref;
        if (req.session.user){
            res.render('main');
        }else{
            res.redirect("/")
        }
        return
        */
        res.render('Main');
    });
    app.post('/ajex', function(req, res) {
        req.seesion.ffkjk='ff';
        res.send('ffdf');
        return
    });
};
